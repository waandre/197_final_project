import React, { Component } from 'react';
import BreadCrumbExampleDivider from './components/nav'

class Header extends Component {
    render() {
      return (
        <header>
            <BreadCrumbExampleDivider/>
        </header>
      );
    }
  }

export default Header
